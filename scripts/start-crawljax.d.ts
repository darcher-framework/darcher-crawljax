import { Logger } from "@darcher/helpers";
export declare function startCrawljax(logger: Logger, chromeDebuggerAddress: string, mainClass: string, timeBudget: number, logDir?: string): Promise<void>;
